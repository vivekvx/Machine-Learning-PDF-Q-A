# Copy this file to .env and fill in your API keys (optional)
# The system runs fully locally with FLAN-T5 — API keys are NOT required.

# Groq API (optional fallback LLM)
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini API (optional fallback LLM)
GEMINI_API_KEY=your_gemini_api_key_here
